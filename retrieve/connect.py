import mysql.connector

mysql_connection = mysql.connector.connect(user='root', 
                              password='root',
                              host='localhost',
                              database='uplytics_db')

